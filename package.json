{
  "name": "blaketarter-2.0",
  "version": "0.1.0",
  "private": true,
  "homepage": ".",
  "dependencies": {
    "react": "15.6.1",
    "react-dom": "15.6.1",
    "smoothscroll-polyfill": "^0.3.6",
    "styled-components": "^2.1.2"
  },
  "devDependencies": {
    "react-app-rewire-preact": "^1.0.1",
    "react-app-rewired": "^1.2.5",
    "react-scripts": "1.0.12",
    "sw-precache": "^5.2.0"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "postbuild": "npm run optimize",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "optimize": "node ./node_modules/sw-precache/cli.js --root='build/' --static-file-globs='build/**/!(*map*)'",
    "deploy": "npm run build && GIT_DEPLOY_DIR=build ./deploy.sh"
  }
}
